# Set this in Vercel's Project Settings -> Environment Variables.
# Do not add a trailing slash.
VITE_API_URL=https://ratelimiter-c2k4.onrender.com

# Google OAuth Client ID (optional if served via /api/config, or set directly for frontend)
VITE_GOOGLE_CLIENT_ID=

